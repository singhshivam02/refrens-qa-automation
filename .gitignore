
# Playwright
node_modules/
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/
/playwright/.auth/

# Debug screenshots — never commit
debug-screenshot.png
debug-*.png
screenshot-*.png

# Auth session — generated by auth.setup.ts, never commit
storageState.json

# All .env files are gitignored — copy .env.example and fill in your values
.env
.env.staging
.env.prod
.env.local

# QA CLI business context — set via `qa use business <urlKey>`, personal per-dev
.qa-context.json

# Claude Code local settings — IDE-specific, not for the repo
.claude/

# INP reports (generated — do not commit)
/reports/raw/
/reports/stage/
/reports/prod/
/reports/comparison/
